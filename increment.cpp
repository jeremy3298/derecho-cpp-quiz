/* Feel free to include any libraries you need */

/**
 * Write a templated function that increments a number *at compile time*. It should
 * only compile for numeric types. (HINT: std::enable_if and std::is_arithmetic)
 *
 * Examples: 
 *  increment(1) -> evaluates to 2
 *  increment("abc") -> fails to compile
 */

/**
 * Call your code and print its result from inside main enough times to convince 
 * yourself it works  
 */
int main() {
}
